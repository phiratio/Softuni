/**
 * Created by Void-S on 27-Feb-17.
 */
function solve(arr) {

    var area = (arr[0] * arr[1] ) / 2;
    area = Number(area.toFixed(2));
    console.log(
        "Triangle area = " + area
    );

}