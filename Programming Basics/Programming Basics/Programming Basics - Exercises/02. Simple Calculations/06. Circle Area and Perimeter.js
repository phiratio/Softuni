/**
 * Created by Void-S on 27-Feb-17.
 */
function solve(arr) {
    var area = Math.PI * arr[0] * arr[0];
    var perimeter = 2 * Math.PI * arr[0];
    console.log(
        "Area = " + area + "\n" +
        "Perimeter = " + perimeter
    );


}